int c2i(char c) { return c; }

// https://c-faq.com/decl/spiral.anderson.html
char str[10];
char *(*fp)(int, float *);
void (*signal(int, void (*fp)(int)))(int);

struct a { char y; char *monkey; };

struct b;

int takesa(struct a x[], int * ff, char f) {
  int a[2];

  return 0;
}

int minusone = -1;

int x(int *());

int brother() {
  int a, b;

  int *other = &minusone;

  if (1) {
    struct a {int y;} q;
  }

  struct a x;
  takesa(&x, 0, 0);
  return 0;
}

int main() {
  // int a = y[10]('c');
  struct a y;
  // int b = (*z('c'))[10];
}
